[build-system]
requires = [
    "setuptools>=64",
    "setuptools-scm>=8",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
search_parent_directories = true
fallback_version = "0.0.0.dev0"
